跳表的实现：
  1、关键在于结构，结构上如何表示跳表的一个节点决定了实现的复杂程度。
      参考http://www.cnblogs.com/xuqiang/archive/2011/05/22/2053516.html中介绍的方式——一个节点携带多个指针的方式；
      
  2、在节点的高度上，选择了使用随机的方式来决定节点的高度，参考的值是头节点的高度——最多只能比头节点高一个层次
  （这样会导致在最开始插入的节点的高度较低，而后面插入的高度会比前面高的可能大,从构造的过程来说可可行的，但是由于中间并没有进行任何的调整
    ，会导致这个问题一直贯穿与整个处理过程，只有在树到达最高时，情况会改变）
  
